
---

## 2. `.env.example`

```env
# ==============================
# Global defaults
# ==============================
# I keep my timezone, base config path, and media path here so I can reuse them
# across the docker-compose file.

TZ=Australia/Canberra
BASE_PATH=/REPLACE_ME/appdata           # where per-service configs live on the host
MEDIA_SHARE=/REPLACE_ME/media          # where your actual media + Downloads live

# ==============================
# Plex config
# ==============================
# PLEX_URL is how the other services (and lb_sync) talk to Plex.
# PLEX_TOKEN is your Plex auth token.
# PLEX_CLAIM is only needed on first run to claim the Plex server to your account.
# PLEX_CLIENT_FILTER (optional) lets me scope to a single Plex client (like Plexamp).

PLEX_URL=http://REPLACE_ME_PLEX_HOST:32400
PLEX_TOKEN=REPLACE_ME_PLEX_TOKEN
PLEX_CLAIM=REPLACE_ME_PLEX_CLAIM
PLEX_CLIENT_FILTER=Plexamp

# ==============================
# User / group IDs for containers
# ==============================
# I run the containers as a known UID:GID on the host so they can all read/write
# the same media folders without permission drama.
# In my setup they’re all 1000:1000, but set these to whatever matches your host user.

RADARR_PUID=1000
RADARR_PGID=1000
SONARR_PUID=1000
SONARR_PGID=1000
PLEX_PUID=1000
PLEX_PGID=1000
READARR_PUID=1000
READARR_PGID=1000
LIDARR_PUID=1000
LIDARR_PGID=1000
PROWLARR_PUID=1000
PROWLARR_PGID=1000
AUTOBRR_PUID=1000
AUTOBRR_PGID=1000
OVERSEERR_PUID=1000
OVERSEERR_PGID=1000
QBITTORRENT_PUID=1000
QBITTORRENT_PGID=1000
TAUTULLI_PUID=1000
TAUTULLI_PGID=1000
TDARR_PUID=1000
TDARR_PGID=1000
BAZARR_PUID=1000
BAZARR_PGID=1000
PLEXAUTOLANGUAGES_PUID=1000
PLEXAUTOLANGUAGES_PGID=1000
PLEXMETAMANAGER_PUID=1000
PLEXMETAMANAGER_PGID=1000
DOZZLE_PUID=1000
DOZZLE_PGID=1000

# ==============================
# ListenBrainz / Lidarr integration
# ==============================
# These are used by lb_sync (run.py) to:
# - poll ListenBrainz or associate plays
# - talk to Lidarr to delete disliked tracks
# - tag, profile, and limit imports
#
# LB_TOKEN is your ListenBrainz token.
# LIDARR_API_KEY is your Lidarr API key.
# LIDARR_URL should be reachable from the lb_sync container.

LB_USER=REPLACE_ME_LISTENBRAINZ_USERNAME
LB_TOKEN=REPLACE_ME_LISTENBRAINZ_TOKEN

LIDARR_URL=http://REPLACE_ME_LIDARR_HOST:8686
LIDARR_API_KEY=REPLACE_ME_LIDARR_API_KEY

# This path MUST match the Lidarr root folder for your music.
MUSIC_ROOT=/media/Plex/Music

# These are the quality/metadata profiles and IDs you use in Lidarr.
QUALITY_PROFILE_NAME=Lossless
METADATA_PROFILE_NAME=Lossless
QUALITY_PROFILE_ID=2
METADATA_PROFILE_ID=1

# Tag info for Lidarr (I tag imported artists so I know they came from automation).
TAG_NAME=listenbrainz
TAG_ID=0
LIDARR_TAG_ID=0

# Safety rails so I don't flood Lidarr with 500 new artists in one hit.
MAX_NEW_ARTISTS=10
MAX_NEW_ALBUMS=40

# Tell Lidarr to run an immediate search after adding the artist.
RUN_SEARCH_AFTER_ADD=true

# What kinds of data I'm tracking/importing. This is mostly here for future logic.
MODES=artists,recordings

# ==============================
# Plex → ListenBrainz scrobbling
# ==============================
# Used by plex-beetbrainz.
# PBZ_PLEX_USER is my Plex username.
# PBZ_LISTEN_THRESHOLD is the % of a track I need to listen to before it "counts."

PBZ_PLEX_USER=REPLACE_ME_PLEX_USERNAME
PBZ_LISTEN_THRESHOLD=75

# ==============================
# VPN / gluetun
# ==============================
# These credentials are used to authenticate gluetun to the VPN provider.
# qBittorrent shares gluetun's network namespace so torrent traffic only routes through VPN.

VPN_SERVICE_PROVIDER=nordvpn
VPN_USERNAME=REPLACE_ME_VPN_USERNAME
VPN_PASSWORD=REPLACE_ME_VPN_PASSWORD
VPN_COUNTRY=Australia

